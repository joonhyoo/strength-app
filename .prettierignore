# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Generated/local-only — not tracked in git, shouldn't be formatted as source.
# Prettier doesn't read .gitignore automatically (unlike eslint.config.js's
# includeIgnoreFile), so these need listing explicitly.
.claude/
supabase/.temp/
supabase/.branches/

# Generated (tracked in git, unlike the above) — output of
# `supabase gen types typescript`, always in the CLI's own 2-space style.
# Reformatting it would just drift back on every regeneration.
src/database.types.ts
